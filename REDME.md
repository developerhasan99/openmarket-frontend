# Openmarket Frontend Project

To start Dev server follow the steps below:

1. Make sure that your computer has nodejs installed.
2. Open the project directory in terminal and run the following command to install dependencies.

    ``yarn``

3. Now run the following command to start dev server.

    ``yarn dev``

4. Finally run the following command to build the project.

    ``yarn build``